{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block content %}

    <div class="signup-intro">
        <h2>Rejoignez notre communauté artistique</h2>
        <p>Créez votre compte pour accéder à :</p>
        <ul>
            <li>🎨 Un espace personnel pour vos projets</li>
            <li>📚 Un guide gratuit pour artistes</li>
            <li>💬 Un accompagnement adapté à vos besoins</li>
        </ul>
    </div>

    {# Flash messages #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <div class="form-container">
        <form method="post">
            {{ form_start(form) }}

            <div class="form-group">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
            </div>

            <div class="form-group">
                {{ form_label(form.firstName) }}
                {{ form_widget(form.firstName) }}
            </div>

            <div class="form-group">
                {{ form_label(form.plainPassword.first, 'Mot de passe') }}
                {{ form_widget(form.plainPassword.first) }}
            </div>

            <div class="form-group">
                {{ form_label(form.plainPassword.second, 'Confirmez le mot de passe') }}
                {{ form_widget(form.plainPassword.second) }}
            </div>

            <div class="form-group">
                {{ form_row(form.cgu) }}
            </div>

            <div class="form-group captcha-check">
                {{ form_row(form.captcha) }}
            </div>

            {{ form_end(form) }}
        </form>
    </div>

    <p>Vous avez déjà un compte ? <a href="{{ path('app_connexion') }}">Se connecter</a></p>
    <a href="{{ path('app_home') }}" class="btn btn-light">Retour à l'accueil</a>

{% endblock %}




